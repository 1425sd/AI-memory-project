# Tech Context

## 技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 语言 | `{{LANGUAGE}}` | `{{VERSION}}` | `{{NOTE}}` |
| 框架 | `{{FRAMEWORK}}` | `{{VERSION}}` | `{{NOTE}}` |
| 数据库 | `{{DATABASE}}` | `{{VERSION}}` | `{{NOTE}}` |
| 部署 | `{{DEPLOYMENT_TARGET}}` | `{{VERSION}}` | `{{NOTE}}` |

## 关键依赖

| 包/服务 | 用途 | 注意事项 |
|---------|------|----------|
| `{{DEPENDENCY_NAME}}` | `{{PURPOSE}}` | `{{NOTE}}` |

## 开发环境

- 操作系统：`{{OS_OR_PLATFORM}}`
- 包管理器：`{{PACKAGE_MANAGER}}`
- 本地服务端口：`{{LOCAL_PORTS}}`
- 环境变量文件：`{{ENV_FILE}}`
- 必需外部服务：`{{EXTERNAL_SERVICES}}`

## 常用命令

安装依赖：

```bash
{{INSTALL_COMMAND}}
```

启动开发环境：

```bash
{{DEV_COMMAND}}
```

类型检查：

```bash
{{TYPECHECK_COMMAND}}
```

代码检查：

```bash
{{LINT_COMMAND}}
```

测试：

```bash
{{TEST_COMMAND}}
```

构建：

```bash
{{BUILD_COMMAND}}
```

## 数据与存储

说明项目使用的数据库、缓存、本地存储、对象存储或文件系统。

- `{{STORAGE_1}}`
- `{{STORAGE_2}}`

## API 与外部服务

- `{{API_OR_SERVICE_1}}`
- `{{API_OR_SERVICE_2}}`

## 构建与部署

- `{{BUILD_RULE_1}}`
- `{{DEPLOY_RULE_1}}`
- `{{RELEASE_RULE_1}}`
